class PaymentsRep {
    constructor() {
    }
    payment = async() => {
        return response
    }
}

const repository = new PaymentsRep()

export default repository

export const { payment } = repository